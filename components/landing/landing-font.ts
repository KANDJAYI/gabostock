import { Inter } from "next/font/google";

/** Police marketing (près des maquettes iOS — Inter). */
export const landingInter = Inter({
  subsets: ["latin"],
  weight: ["400", "500", "600", "700", "800"],
  display: "swap",
  variable: "--font-landing-inter",
});
